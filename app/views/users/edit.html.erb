<h1><strong>Edit user </strong></h1>

 <% if @user.errors.any? %>
    <%= the following errors prevented user from being saved %>
    <% @user.errors.full_massages.each do|msg| %>
        <%= msg %>
     <% end %>
  <% end %>

  <%= form_with model: @user, url: user_path(@user.id), method: :put do |form| %>

    <p>
            <%= form.label :username %>
            <%= form.text_field :username %>
    </p>
    
    <p>
            <%= form.label :email %>
            <%= form.text_field :email %>
    </p>
   
    <p>
            <%= form.submit %>
    </p>
<% end %>
<%= link_to 'Go to users list', users_path %>





